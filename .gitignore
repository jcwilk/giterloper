.giterloper/
.giterloper_test/

# Unified test harness orchestrator lock (git-ed8c); must stay outside session trees
.giterloper-harness.lock
.giterloper-harness.meta.json
.giterloper-harness.meta.json.*

# Python venv (memsearch, local MCP); recreate with `python3 -m venv .venv && pip install memsearch`
.venv/

.env

.cursor/plans/

logs/giterloper-retry.log

# Per-harness MCP test session parent trees (git-jp1p); allocated under tests/roots/giterloper-test-runs/
tests/roots/

# OpenAI VCR rerecord-all coordination (exclusive create; not part of committed tapes)
tests/fixtures/.openai-vcr-rerecord-once
