# MCP documentation (non-normative)

The **canonical** MCP product contract is [`specs/MCP.md`](specs/MCP.md).

This file remains at the repository root so older links to `MCP.md` still resolve; it does not define behavior.
