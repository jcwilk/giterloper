{
  "name": "giterloper",
  "version": "0.1.0",
  "description": "Git-based knowledge store manager",
  "type": "module",
  "scripts": {
    "test:unit": "deno test -A tests/unit/",
    "test:e2e": "deno run -A scripts/run-e2e.ts",
    "benchmark:embed": "deno run -A scripts/benchmark-embed.ts"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
