{
  "name": "giterloper",
  "version": "0.1.0",
  "description": "Git-based knowledge store manager",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "gl": "node .cursor/skills/gl/scripts/gl.mjs",
    "build": "npx tsc -p .cursor/skills/gl",
    "test:unit": "npm run build && node --experimental-strip-types --test tests/unit/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "benchmark:embed": "node scripts/benchmark-embed.mjs"
  },
  "dependencies": {
    "@tobilu/qmd": "1.1.5"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.7"
  }
}
