# Benchmark Test Document A

This document exists for reproducible embedding benchmarks. It describes giterloper concepts.

Giterloper is a Git-backed knowledge storage system. Knowledge is stored in markdown files under a `knowledge/` directory. The system supports semantic search via vector embeddings.

Key operations: clone, index, search, query, add, subtract, reconcile, promote. The embedding step converts document chunks into vectors for similarity search.
